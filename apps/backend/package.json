{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p tsconfig.json && node ./scripts/copy-frontend-to-root-dist.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.0",
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2"
  }
}
